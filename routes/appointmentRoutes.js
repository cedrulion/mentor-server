const express = require('express');const {   requestAppointment,   respondToAppointment,   getAllAppointments,   getAppointmentById,   getAppointmentsByUserId,   deleteAppointment,   getAppointmentsForLoggedInUser } = require('../controllers/appointmentController');const router = express.Router();const passport = require('passport');// Route to request an appointmentrouter.post('/appointments', passport.authenticate('jwt', { session: false }), requestAppointment);// Route to respond to an appointmentrouter.post('/appointments/respond', passport.authenticate('jwt', { session: false }), respondToAppointment);// Route to get all appointmentsrouter.get('/appointments', passport.authenticate('jwt', { session: false }), getAllAppointments);// Route to get a specific appointment by IDrouter.get('/appointments/:id', passport.authenticate('jwt', { session: false }), getAppointmentById);// Route to get appointments for a specific user by user IDrouter.get('/appointments/user/:userId', passport.authenticate('jwt', { session: false }), getAppointmentsByUserId);// Route to delete an appointment by IDrouter.delete('/appointments/:id', passport.authenticate('jwt', { session: false }), deleteAppointment);// Route to get appointments for the logged-in userrouter.get('/appointments/user', passport.authenticate('jwt', { session: false }), getAppointmentsForLoggedInUser);module.exports = router;